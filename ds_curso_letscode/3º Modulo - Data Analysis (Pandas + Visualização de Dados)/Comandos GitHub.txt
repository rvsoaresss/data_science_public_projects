git config --global user.name "rvsoaresss"
git config --global user.email "rodrigosoares.s@hotmail.com"
git init (cria uma pasta no repositorio, normalmente fica oculta)
git remote add origin https://github.com/rvsoaresss/dscourse.git (faz a sincronização com o servidor do github)
git pull origin master
git add . (adiciona todos os arquivos de uma vez)
git commit -m "versão 0.1" (comita as pastas)
git push origin master (envia para o servidor)


git checkout origin/master -- 2011-11-25 %Debug.ipynb







